# 夜计划检查记录

**配置来源**：`<check-config 输出的 source>`　**检查时间**：`<UTC>`

## 配置

| 项 | 值 | 结论 |
| --- | --- | --- |
| 时间窗合计 | `<h>` 小时 | |
| d_moon | `<deg>` 度 | |
| 滤镜 | `<filters>` | |
| 曝光 | `<count> × <time> s，间隔 <wait> min` | |

error `<n>` 个，warning `<n>` 个。<有 error 就写"未通过，先修配置"。>

## 预算

单目标 `<min>` 分钟（含 `<slew>` 秒转向开销），一夜 `<h>` 小时 → 容量 `<n>` 个目标。

容量是几何上界：不含高度角、月亮规避、天气和转向路径，真实可排数更少。

## 目标表

`<csv>`，`<n>` 个目标，容量 `<cap>`。

| 级别 | 位置 | 问题 |
| --- | --- | --- |
| | | |

## 结论

<能不能交、还缺什么。若涉及真实栈，列出仍然缺失的前置条件，不要写成已就绪。>
